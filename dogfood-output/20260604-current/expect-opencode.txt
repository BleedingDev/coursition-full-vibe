
 expect v0.0.23  CI · opencode · timeout 15m

 Be merciless and brutal. Test Coursition end to end as a real course creator. Cover: dashboard course management (create, edit, delete), logo/home navigation, manual mode, full AI generated mode using https://junior.guru/handbook/, source processing readiness, questions/topics/target/chapters/lessons/preview gates, localized UI and stable localized URLs, SSR/no-JS initial loading, browser back/forward/reload, no stale state overriding the URL, no technical implementation copy, no confusing placeholder styling, no broken or over-tall buttons, no borders/cards/shadows clutter, no duplicate persistent errors. Report concrete reproducible UX and functional issues only, with severity and steps.
 http://localhost:8080
 ◯ Open app and inspect dashboard data state
 ✔ Auth gate is reachable at `/en`; account submission left the page on the sign-up form with `Create account` disabled and no console errors, establishing the initial blocked state to investigate through adjacent flows (17s)
 ◯ Validate sign-up submission failure state and capture reproducible bug evidence
 ✔ Sign-up actually succeeded after async settle: URL stayed `/en`, page title remained `Coursition course generator`, and the dashboard rendered `Courses` with a visible `Create course` button and empty-state management text (9s)
 ◯ [Setup] Seed three courses and verify dashboard persistence with real data
 ✘ [Setup] Seed three courses and verify dashboard persistence with real data (7s)
   category=app-bug; domain=general; expected=Creating a course should persist each new record immediately without runtime fetch failures; actual=all 3 courses eventually appeared, but every create emitted a React `TypeError: Failed to fetch` from route data loading, indicating the dashboard mutation path is partially broken and recovers only after manual revisit; url=http://localhost:8080/en; evidence=console logged 3 identical `TypeError: Failed to fetch` errors during create while final dashboard body still listed the 3 seeded courses; repro=sign up, create three courses from dashboard form, watch console during each submit; likely-scope=changed dashboard course management/data loader path; next-agent-prompt=Investigate why dashboard course creation triggers `Failed to fetch` in the route loader even though the course record eventually persists.
 ◯ Test localized wizard mode route, logo/home navigation, and back-forward state stability
 ✘ Test localized wizard mode route, logo/home navigation, and back-forward state stability (1m 7s)
   category=app-bug; domain=general; expected=Logo navigation, browser back, and reload should keep UI state aligned with the localized URL for the current wizard step; actual=after opening `/en/course-creation/.../chapters`, clicking the logo returned to `/en`, but browser Back restored URL `/en/course-creation/.../chapters` while the page content still showed the dashboard `Courses` view, proving stale UI state overriding the URL; url=http://localhost:8080/en/course-creation/course_215928c726594b6d8a558167455719aa/chapters; evidence=Playwright captured `backUrl` as the chapters route while `backHeading` was `Courses`; reload then corrected the heading back to `Chapters`; repro=open course edit, switch to manual path until `Chapters`, click logo, then browser Back; likely-scope=localized router/history state for course creation pages; next-agent-prompt=Fix route-state hydration so browser Back restores the matching wizard screen instead of leaving dashboard content mounted under a course-creation URL.
 ◯ Test manual mode gating from mode to sources and chapter prerequisites
 ✔ Manual mode route advanced to `/en/course-creation/.../sources`; the wizard showed `Sources`, offered both `Next` and `Continue`, and displayed guidance that the creator can continue without source material, confirming this branch is intentionally ungated at the sources step (7s)
 ◯ Verify question and topic gates reject empty progression in manual flow
 ✘ Verify question and topic gates reject empty progression in manual flow (10s)
   category=app-bug; domain=general; expected=Questions/topics gates should prevent advancing to later steps until required content exists, especially when the developer request explicitly calls out questions/topics/target/chapters/lessons/preview gates; actual=manual flow allowed direct progression from empty `Questions` to `/topics`, and the resulting `Topics` screen exposed a further `Next` link to `/target-learner` even though `0 topics accepted`; url=http://localhost:8080/en/course-creation/course_215928c726594b6d8a558167455719aa/topics; evidence=Playwright recorded URL `/topics`, body text `0 topics accepted`, and `nextHref` `/en/course-creation/.../target-learner` with no validation message; repro=create course, select `I'll build manually`, continue from Sources, click Next from empty Questions; likely-scope=wizard gate logic for questions/topics progression; next-agent-prompt=Enforce wizard gating so empty questions/topics cannot advance to later course-creation steps.
 ◯ Test localized URLs and Czech UI for course wizard navigation
 ✘ Test localized URLs and Czech UI for course wizard navigation (10s)
   category=app-bug; domain=general; expected=Switching to Czech should localize both the URL and all visible creator UI copy with no English implementation text leaks; actual=the URL localized correctly to `/cs/tvorba-kurzu/.../rezim`, but page text still exposed English copy such as `Course generator`, `Create the first full course from the source material.`, `Save title`, and `Review`; url=http://localhost:8080/cs/tvorba-kurzu/course_40e788f7e2d74cc9a28f71e39516053e/rezim; evidence=snapshot shows Czech labels like `Režim` and `Další` mixed with English hero and control text; repro=open course wizard in English, switch language to `Čeština`; likely-scope=localized creator shell/wizard copy or SSR hydration mismatch; next-agent-prompt=Fix mixed-language rendering so Czech course-creation routes render fully localized copy without English leakage.
 ◯ Test full AI-generated mode with URL source and readiness progression
 ◯ Retry AI mode source flow with fresh refs after mode switch
 ◯ Add URL source from junior.guru handbook and verify processing readiness behavior
 ✔ AI mode accepted `https://junior.guru/handbook/` as a URL source, showed `Junior Guru Handbook` with `URL - Ready - 6097 characters`, and exposed both `Next` and `Continue`, confirming source processing reached a ready state (8s)
 ✔ Source preview opened inline on the sources step and rendered a long extracted excerpt under `Junior Guru Handbook preview`, proving preview is wired to processed URL content without leaving the localized route (20s)
 ◯ Verify AI flow prefilled questions and topic-generation gate behavior
 ✘ Verify AI flow prefilled questions and topic-generation gate behavior (13s)
   category=app-bug; domain=general; expected=Full AI mode should progress beyond processed source readiness by generating questions/topics or at least exposing a working topic-generation action; actual=after source ingestion prefilled all teaching-question fields and checked `Use only source-backed claims`, clicking `Generate topics` never changed route or content, then left both `Save questions` and `Generate topics` disabled on the same `Questions` step; url=http://localhost:8080/en/course-creation/course_b19c670c86684a7097d55295ab5f827f/questions; evidence=fields were auto-populated from the source, but 2 seconds after pressing `Generate topics` the URL stayed `/questions`, status still said `Generate topics before continuing.`, and snapshot showed both action buttons disabled; repro=create AI-assisted course, add `https://junior.guru/handbook/` URL source, continue to Questions, click `Generate topics`; likely-scope=AI generation mutation or pending-state handling on questions step; next-agent-prompt=Fix the `Generate topics` action in AI mode so it either generates topics and advances state or surfaces an explicit error instead of silently disabling the controls.
 ◯ Test dashboard delete behavior and duplicate persistent error handling
 ✔ Delete uses a two-step confirmation on the dashboard: first click changed the control to `Confirm delete` while the course card remained visible, proving the destructive action is guarded instead of firing immediately (8s)
 ✔ Confirmed delete completed on second click: the dashboard stayed at `/en`, the deleted course heading disappeared, and only the two remaining seeded courses were listed afterward (1m 32s)
 ◯ Run responsive mobile quality check on wizard layout and controls
 ✔ At `375x812`, the chapters wizard stayed within viewport width (`scrollWidth` 375 equals `clientWidth` 375), the route remained `/en/course-creation/.../chapters`, and the primary action area including `Confirm structure` stayed visible without horizontal overflow (7s)
 ◯ Test touch interaction on mobile for logo navigation
 → Test touch interaction on mobile for logo navigation [skipped]
   Touch testing requires a touch-enabled browser context; the current browser session was launched without `hasTouch`, so this mandatory mobile tap check could not be executed in-place without restarting the session
 ◯ Run cross-browser primary flow in WebKit
 ✘ Run cross-browser primary flow in WebKit (20s)
   category=app-bug; domain=cross-browser; expected=Primary sign-up flow should succeed in WebKit and reveal the dashboard or a localized post-auth screen; actual=in WebKit, submitting valid sign-up data left the user on the same authentication form at `/en` with `Create account` disabled and no success/error feedback; url=http://localhost:8080/en; evidence=after clicking `Create account`, snapshot still showed the `Authentication mode` form instead of `Courses`, with entered values retained and button disabled; repro=open `/en` in WebKit, fill valid sign-up fields, click `Create account`; likely-scope=auth submission or fetch handling specific to WebKit; next-agent-prompt=Debug why valid sign-up submissions hang on the initial form in WebKit without showing success or failure state.
 ◯ Run accessibility audit on changed creator UI
 ✘ Run accessibility audit on changed creator UI (6s)
   category=app-bug; domain=accessibility; expected=Changed creator UI should not ship serious WCAG issues; actual=accessibility audit reported 2 serious violations on the main flow, including color-only state communication and sensory wording that depends on the word `above`; url=http://localhost:8080/en; evidence=IBM Equal Access flagged `style_color_misuse` on the stylesheet-driven UI state and `text_sensory_misuse` on the paragraph `No courses yet. Create the first one above.`; repro=open `/en`, run accessibility audit on `main`; likely-scope=creator dashboard styling and empty-state copy; next-agent-prompt=Fix the serious accessibility issues on the course dashboard, especially the color-only state communication and the sensory instruction `Create the first one above.`
 ◯ Collect performance metrics for creator surface
 ✔ Performance trace passed: FCP/LCP were 215ms, CLS was 0, no poor web vitals appeared, and the trace showed no blocking long animation frames over the failure threshold (3s)
 ◯ Run project healthcheck scripts from package manifest
 Still running… (8m elapsed)
 Still running… (9m elapsed)
 ✔ Project healthcheck reached tests and failed on `tests/ultramodern.contract.test.ts` because `.modernjs/ultramodern-package-source.json` was missing; formatting, lint, typecheck, and i18n checks passed beforehand (2m 56s)

 Tests  10 passed | 7 failed | 1 skipped (18)
 Time   2ms
